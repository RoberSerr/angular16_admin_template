import { Routes } from '@angular/router';
import { ChartsComponent } from '@modules/main/pages/charts/charts.component';

export const chartsRoutes: Routes = [
    {
        path: '',
        component: ChartsComponent
    }
]