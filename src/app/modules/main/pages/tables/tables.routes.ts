import { Routes } from '@angular/router';
import { TablesComponent } from '@modules/main/pages/tables/tables.component';

export const tablesRoutes: Routes = [
    {
        path: '',
        component: TablesComponent
    }
]