import { TablesSortableDirective } from './tables-sortable.directive';

describe('TablesSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new TablesSortableDirective();
    expect(directive).toBeTruthy();
  });
});
